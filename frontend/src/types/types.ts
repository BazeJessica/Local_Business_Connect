export interface businesseType {
  id: number;
  name: string;
  category: string;
  description: string;
  img: string;
}

export interface categoryType {
  id: number;
  name: string;
  description: string;
  img: string;
  url: string;
}
